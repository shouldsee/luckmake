language: python
# services:
  # - go
  # - singularity
python:
  # - "2.7"
  # - "3.5"
  - "3.7"

# cache:
#   directories: 
#     - $HOME/.pip
#     # - $HOME/virtualenv ### too big
before_install: [] 
install:
  - echo pass
  - python -V
  - pip install pyinstaller
  - pip install .
script:
  - set -e
  - bash test.sh
  - pytest .
  - bash build.sh
  - bin/luck --help
  - bin/luckbd --help
  # - python test.py --all
  # - ls -lhtr /deps/*
  # - tree /deps
