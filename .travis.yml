language: python
# services:
  # - go
  # - singularity
python:
  # - "2.7"
  - "3.5"
  - "3.7"
cache:
  directories: 
    - $HOME/.pip
    # - $HOME/virtualenv ### too big
before_install: [] 
install:
  - echo pass
script:
  - python -V
  - pip install .
  - pytest .
  # - python test.py --all
  # - ls -lhtr /deps/*
  # - tree /deps
